<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <!-- Tell the system this app requires OpenGL ES 3.0 -->
    <uses-feature android:glEsVersion="0x00030000" android:required="true" />

    <application
            android:name=".OpenGLScenesApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.LearnOpenGLScenes"
            tools:ignore="AllowBackup,GoogleAppIndexingWarning">

        <activity android:name=".ui.InfiniteCubeActivity"
                  android:theme="@style/Theme.LearnOpenGLScenes.NoActionBar"
                  android:configChanges="orientation|screenSize"
                android:exported="true"/>
        <activity android:name=".ui.InfiniteCapsulesActivity"
                  android:theme="@style/Theme.LearnOpenGLScenes.NoActionBar"
                  android:configChanges="orientation|screenSize"
                android:exported="true"/>
        <activity android:name=".ui.MandelbrotActivity"
                android:theme="@style/Theme.LearnOpenGLScenes.NoActionBar"
                android:configChanges="orientation|screenSize"
                android:exported="true"/>
        <activity android:name=".ui.MengerPrisonActivity"
                android:theme="@style/Theme.LearnOpenGLScenes.NoActionBar"
                android:configChanges="orientation|screenSize"
                android:exported="true"/>
        <activity
                android:name=".ui.HomeActivity"
                android:configChanges="orientation|keyboardHidden|screenSize"
                android:theme="@style/Theme.LearnOpenGLScenes.NoActionBar"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>